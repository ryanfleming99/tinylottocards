/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: "", // Remove if unnecessary
  trailingSlash: true // Ensures paths work with static exports
};

module.exports = nextConfig;
